const times=['08:00','09:30','11:00','14:00','15:30','17:00'],key='cuidar-appointments';const timeBox=document.querySelector('#times'),read=()=>JSON.parse(localStorage.getItem(key)||'[]'),write=data=>localStorage.setItem(key,JSON.stringify(data));times.forEach((time,i)=>timeBox.insertAdjacentHTML('beforeend',`<label><input type="radio" name="time" value="${time}" ${i===0?'required':''}><span>${time}</span></label>`));function safe(value){const d=document.createElement('div');d.textContent=value;return d.innerHTML}function render(){const data=read(),query=document.querySelector('#search').value.toLowerCase(),filter=document.querySelector('#appointment-filter').value,visible=data.filter(i=>(filter==='all'||i.status===filter)&&(`${i.name} ${i.specialty}`.toLowerCase().includes(query))),section=document.querySelector('#appointments'),list=document.querySelector('#list');section.hidden=!data.length;list.innerHTML='';visible.forEach(item=>{const li=document.createElement('li'),cancelled=item.status==='Cancelado';li.innerHTML=`<div class="${cancelled?'cancelled':''}"><strong>${safe(item.specialty)} · ${safe(item.name)}</strong><p>${safe(item.doctor)} · ${safe(item.mode)}</p><p>${new Date(item.date+'T12:00').toLocaleDateString('pt-BR')} às ${safe(item.time)}</p></div><div><span>${item.status}</span><button data-cancel="${item.id}" ${cancelled?'disabled':''}>Cancelar</button><button data-delete="${item.id}" aria-label="Excluir solicitação">Excluir</button></div>`;list.append(li)});document.querySelector('#appointment-empty').hidden=visible.length>0;document.querySelector('#total-count').textContent=data.length;document.querySelector('#online-count').textContent=data.filter(i=>i.mode==='Online').length}document.querySelector('#form').addEventListener('submit',e=>{e.preventDefault();const item={...Object.fromEntries(new FormData(e.currentTarget)),id:crypto.randomUUID(),status:'Confirmado',createdAt:new Date().toISOString()},data=read();data.unshift(item);write(data);document.querySelector('#status').textContent=`Solicitação registrada para ${item.date.split('-').reverse().join('/')} às ${item.time}.`;e.currentTarget.reset();render()});document.querySelector('#list').addEventListener('click',e=>{const data=read(),cancel=e.target.dataset.cancel,del=e.target.dataset.delete;if(cancel){data.find(i=>i.id===cancel).status='Cancelado';write(data)}if(del)write(data.filter(i=>i.id!==del));render()});document.querySelector('#clear').addEventListener('click',()=>{if(confirm('Apagar todo o histórico?')){localStorage.removeItem(key);render()}});document.querySelector('#search').addEventListener('input',render);document.querySelector('#appointment-filter').addEventListener('change',render);document.querySelector('input[type=date]').min=new Date().toISOString().split('T')[0];render();
